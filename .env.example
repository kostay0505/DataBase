# PostgreSQL
DATABASE_URL="postgresql://pigulevsky:password@localhost:5432/pigulevsky_db"

# JWT (generate: openssl rand -base64 64)
JWT_SECRET="your-super-secret-jwt-key-min-32-chars"

# Admin credentials
ADMIN_LOGIN="admin"
# Generate hash: node -e "const b=require('bcryptjs');b.hash('yourpassword',12).then(console.log)"
ADMIN_PASSWORD_HASH="$2a$12$..."

# App
APP_URL="https://databasepigulevsky.ru"
NODE_ENV="production"
